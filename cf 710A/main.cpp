#include <bits/stdc++.h>

using namespace std;

int main()
{
    char p;
    int q;
    cin>>p,q;
    //if((p=='a' ||'h') && (q==1||8))
    if('a'1||'h'1||'a'8||'h'8)
        {
            cout<<3;
            return 0;
        }
    if((p=='a') || (p=='h') ||(q==1) || (q==8))
        {
        cout<<5;
        return 0;
        }
    else
        {
            cout<<8;
            return 0;
        }
}
